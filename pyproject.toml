[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "danfse-brasil"
version = "0.1.0"
description = "Geracao do DANFSe conforme as notas tecnicas da NFS-e nacional."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Projeto DANFSe" }]
dependencies = [
  "qrcode[pil]>=7.4",
  "weasyprint>=61.0",
]

[project.scripts]
danfse-brasil = "danfse_brasil.cli:main"
danfse-nt008 = "danfse_brasil.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/danfse_brasil"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
